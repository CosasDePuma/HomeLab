---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "name" . }}
  {{- include "metadata" . | nindent 2 }}
spec:
  selector:
    app: {{ include "name" . }}
  ports:
  - name: {{ include "name" . }}
    port: 9091
    protocol: TCP
    targetPort: {{ include "shortName" . }}
...