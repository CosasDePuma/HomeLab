---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name:  {{ include "name" . }}
  {{- include "metadata" . | nindent 2 }}
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: {{ .Values.wgEasy.route }}
    services:
    - name: {{ include "shortName" . }}
      port: {{ include "shortName" . }}-webui
---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name:  {{ include "name" . }}
  {{- include "metadata" . | nindent 2 }}
spec:
  entryPoints:
  - wireguard
  routes:
  - kind: Rule
    match: {{ .Values.wgEasy.route }}
    services:
    - name: {{ include "name" . }}
      port: {{ include "shortName" . }}-vpn
...